version: '3.8'

services:
  skyimage:
    image: fishcpy/skyimage:latest
    container_name: skyimage
    restart: unless-stopped
    ports:
      - "8080:8080"
    environment:
      - HTTP_ADDR=:8080
      - DATABASE_TYPE=sqlite
      - DATABASE_PATH=storage/data/skyimage.db
      - STORAGE_PATH=storage/uploads
      - PUBLIC_BASE_URL=http://localhost:8080
      - JWT_SECRET=skyImage(可换为任意文本)
      - ALLOW_REGISTRATION=true
      - FRONTEND_DIST=dist
      - TZ=Asia/Shanghai
    volumes:
      - ./storage/data:/app/storage/data
      - ./storage/uploads:/app/storage/uploads
      # 重要：.env 文件持久化，保存安装后的数据库配置
      - ./.env:/app/.env
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:8080/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # 可选：使用 MySQL 数据库
  # mysql:
  #   image: mysql:8.0
  #   container_name: skyimage-mysql
  #   restart: unless-stopped
  #   environment:
  #     - MYSQL_ROOT_PASSWORD=${MYSQL_ROOT_PASSWORD}
  #     - MYSQL_DATABASE=skyimage
  #     - MYSQL_USER=skyimage
  #     - MYSQL_PASSWORD=${MYSQL_PASSWORD}
  #   volumes:
  #     - mysql-data:/var/lib/mysql
  #   ports:
  #     - "3306:3306"

  # 可选：使用 PostgreSQL 数据库
  # postgres:
  #   image: postgres:16-alpine
  #   container_name: skyimage-postgres
  #   restart: unless-stopped
  #   environment:
  #     - POSTGRES_DB=skyimage
  #     - POSTGRES_USER=skyimage
  #     - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
  #   volumes:
  #     - postgres-data:/var/lib/postgresql/data
  #   ports:
  #     - "5432:5432"

# volumes:
#   mysql-data:
#   postgres-data:
